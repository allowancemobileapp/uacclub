export type Program = {
  slug: string;
  title: string;
  description: string;
  longDescription: string;
  imageId: string;
};

export type Event = {
  id: string;
  title: string;
  date: string;
  description: string;
  imageId: string;
};
